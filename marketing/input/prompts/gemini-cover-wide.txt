Create a single wide hero image 7680x1080px showing a continuous single-word title stretched across four equal panels (each 1920x1080).

Instructions:
- Background: brand gradient from #0A73FF to #FFB800, soft cinematic lighting, minimal geometric shapes.
- Place a modern mobile device mockup centered in each panel with realistic reflections and drop shadows. Each device shows a different high-resolution app screenshot (provide screenshots separately).
- Text style: bold geometric sans, white letters with a subtle 1-2px dark stroke and a soft drop shadow. Ensure Arabic fonts support joining forms if the stretched word is Arabic.
- Safe bleed: keep 120px bleed on left/right edges of each panel for safe cropping.
- Output required: `cover-full-wide.png` (7680x1080) + `cover_word.svg` (vector outline of the stretched word) + 2-3 variation sets.

Rendering notes:
- Ask Gemini to output a high-resolution PNG and also provide a layered source (PSD/Figma) if possible.
- Request 3 variations (lighting/composition/color temperature) and a copy with no overlaid text (for thumbnails).
- After receiving `cover-full-wide.png`, split it into four equal panels (1920x1080 each) to produce `cover-01.png` … `cover-04.png` so the word remains continuous across panels.

Example prompt (English):
"Create a single wide hero image 7680x1080px showing a continuous single-word title stretched across four equal panels (each 1920x1080). Background: brand gradient from #0A73FF to #FFB800, soft cinematic lighting, minimal geometric shapes. Place a modern mobile device mockup centered in each panel with realistic reflections and drop shadows, each showing a different high-resolution app screenshot. Text style: bold geometric sans, white letters with a subtle 1-2px dark stroke and a soft drop shadow. Keep a 120px bleed on left/right edges. Photorealistic, marketing-grade. Export as PNG and provide layered vector for the stretched word. Provide 3 variations."

Example prompt (Arabic):
"انشئ صورة واسعة 7680x1080px تعرض كلمة واحدة مستمرة ممتدة عبر أربع لوحات متساوية (كل لوحة 1920x1080). الخلفية: تدرج لوني من #0A73FF إلى #FFB800، إضاءة سينمائية ناعمة، أشكال هندسية بسيطة. ضع نموذج جهاز موبايل حديث في وسط كل لوحة مع انعكاسات وظلال ناعمة، وكل جهاز يعرض شاشة مختلفة من التطبيق بجودة عالية. نمط الخط: سانس هندسي عريض، الحروف باللون الأبيض مع حد dark stroke خفيف وظل ناعم. اترك bleed 120px على الحواف. أخرج PNG وvector للكلمة الممدودة وقدم 3 نسخ متباينة."

Suggested filenames to request/expect:
- cover-full-wide.png (7680x1080)
- cover_word.svg (vector outline)
- cover-no-text.png (variant)
- cover-01.png … cover-04.png (1920x1080 each, split from wide)

Notes about Arabic continuity:
- If the stretched word is Arabic, request the generator to provide the stretched word as a vector outline to retain correct joining and avoid glyph-breaking when split.
